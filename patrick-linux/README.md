# Patrick Linux

Linux-native desktop adapter built with Python and Tk. The UI is separate from the Unix-socket context protocol, so other operating systems can provide their own UI adapter without changing Patrick Core or the Obsidian context payload.
